[project]
name = "paint-rag"
version = "0.1.0"
description = "RAG system for paint shop knowledge"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "pytest>=8.0",
    "reportlab>=4.0",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]